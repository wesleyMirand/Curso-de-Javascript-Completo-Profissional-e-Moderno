const cursosTodos = document.getElementsByClassName("curso")

console.log(cursosTodos)

/* 

metodo getElementByClassName : permite obter elementos do dom, de classes especificas.
retorna conjunto de elementos também.
vou pegar todos elemento que usam a classe curso.
foi retornado html collection com todos arrays disponiveis(tamanho12).
como levo isso para um array operar de forma melhor o contorno dos metodos disponiveis,
para array, usar um for it, metodo tradicionais podemos!

*/