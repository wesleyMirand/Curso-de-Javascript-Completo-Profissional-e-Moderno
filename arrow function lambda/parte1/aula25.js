const soma = (v1,v2) => {return v1 + v2}

console.log(soma(10,15))


/* 

ArrowFunction: lambda funcao anonima, conceito arrowfunction,
usa anotacao que lembre uma setinha.

precisamos associar funcao a var, pode ser const.
const soma = function(v1,v2) {return v1+ v2} PADRAO vamos passar
para arow function:

precisa entra com argumentos operar e volta com return
funcao de  =>

const soma=(v1, v2) => {return v1 + v2}

*/